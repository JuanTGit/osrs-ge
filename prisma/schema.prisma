generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
}

// model PriceHistory {
// 	id int @id @default(autoincrement())
// 	itemName String
// 	itemId Int
// }

model PriceHistory {
    id        Int      @id @default(autoincrement())
    itemName  String
    itemId    Int
    high      Int
    low       Int
    createdAt DateTime @default(now())
}
